CREATE TABLE eclipseOld(bug_id varchar(25), short_desc varchar(500));

CREATE TABLE mozillaOld (
    bug_id varchar(25),
    short_desc varchar(500)
);

CREATE TABLE eclipse (
    bug_id varchar(25),
    short_desc varchar(500)
);

CREATE TABLE kibana (
    bug_id varchar(25),
    short_desc varchar(500)
);

CREATE TABLE vscode (
    bug_id varchar(25),
    short_desc LONGTEXT
);

CREATE TABLE mozilla (
    bug_id varchar(25),
    short_desc varchar(500)
);

CREATE TABLE spark (
    bug_id varchar(25),
    short_desc varchar(500)
);

CREATE TABLE hadoop (
    bug_id varchar(25),
    short_desc varchar(500)
);